<UserControl x:Class="ZenTimings.Controls.AppIcon"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             x:Name="Root">
    <Path Data="{Binding Data, ElementName=Root}"
          Fill="{Binding Fill, ElementName=Root}"
          Stretch="Uniform"
          Width="{Binding IconWidth, ElementName=Root}"
          Height="{Binding IconHeight, ElementName=Root}"
          HorizontalAlignment="Center"
          VerticalAlignment="Center"/>
</UserControl>
